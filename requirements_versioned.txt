jupyter==1.0.0
latextable==1.0.0
matplotlib==3.7.2
mlflow==3.14.0
numpy==1.24.4
optuna==4.9.0
pandas==2.0.3
pyyaml==6.0.1
scienceplots==2.1.0
scikit-learn==1.3.0
scipy==1.10.1
seaborn==0.12.2
texttable==1.6.7
torch==2.0.1+cu118
tqdm==4.66.1

# torch is a CUDA build, not on plain PyPI - install it explicitly first with:
#   pip install torch==2.0.1 --index-url https://download.pytorch.org/whl/cu118
# then `pip install -r requirements_versioned.txt` will find it already satisfied.
# (`overrides` was removed - see README "Adjustments to the MILLET code" #2 - it's not
# installed or imported anywhere in this env.)
