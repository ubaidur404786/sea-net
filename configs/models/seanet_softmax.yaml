# seanet_softmax.yaml - SEA-Net encoder + our "softmax_conjunctive" pooling head.
#
# A copy of seanet.yaml with ONLY the pooling head changed (fair A/B vs the Conjunctive baseline).
# This head turns the attention into a real probability distribution OVER TIME (softmax, so the
# weights sum to 1) with a learnable temperature, instead of an unnormalised sigmoid gate divided by
# the series length. That makes long and short series comparable and lets the model concentrate on the
# few decisive time points. Run it with:
#   python main.py run --model seanet_softmax
#   python main.py run --model seanet_softmax --dataset Coffee --smoke

name: seanet_softmax

# ---- the encoder (identical to seanet.yaml) ----
encoder:
  type: mstcn_sep
  d: 128
  n_blocks: 6
  dropout: 0.2
  max_dilation: 16
  kernels: [5, 11, 23]

# ---- the pooling head (THE ONLY DIFFERENCE vs seanet.yaml) ----
pooling:
  type: softmax_conjunctive     # attention normalised over time (a distribution) + learnable temperature
  d_attn: 8                     # width of the small attention MLP
  dropout: 0.2
  positional_encoding: true
  temperature: 1.0              # STARTING temperature (it is a learnable parameter; this is just its
                                # initial value). >1 = smoother/more uniform weights (~ mean pooling),
                                # <1 = sharper/peakier weights.

# ---- the training recipe (identical to seanet.yaml) ----
training:
  n_epochs: 400
  patience: 60
  max_batch: 16
  learning_rate: 0.00125
  weight_decay: 0.00012
  label_smoothing: 0.13
  lambda_entropy: 0.01
  min_train_for_val: 100
  val_frac: 0.2
  optimizer: adam

# ---- Optuna hyperparameter search (disabled by default) ----
# Run with:  python main.py optuna --model seanet_softmax
# temperature is LEARNED during training, so it is not in the search space below. If you want Optuna
# to tune its INITIAL value too, add under search_space:  pooling: {temperature: {type: float, low:
# 0.2, high: 5.0, log: true}}  - it works because build_pooling reads pooling.temperature from config.
optuna:
  enabled: false
  n_trials: 20
  timeout: null
  sampler: tpe
  pruner: median
  save_best_parameters: true
  search_space:
    training:
      learning_rate: {type: float, low: 0.0003, high: 0.005, log: true}
      weight_decay: {type: float, low: 0.00001, high: 0.0005, log: true}
      label_smoothing: {type: float, low: 0.0, high: 0.2}
      lambda_entropy: {type: float, low: 0.0, high: 0.05}
    encoder:
      dropout: {type: float, low: 0.1, high: 0.4}
