# seanet/seanet_slim_gatedattn.yaml
# Plain (un-gated) encoder + the NEW gated-attention pooling head. Isolates the pooling: compare against
# seanet/seanet_slim (same encoder, classwise_conjunctive) to see the gated-attention effect on its own.
#
# ENCODER = mstcn_sep: the plain multi-scale separable TCN (no gate) - clean baseline encoder.
#
# POOLING = gated_attention  (Ilse et al. 2018 gated-attention MIL; the MS-DA-MIL paper-1 family)
#   For each timestep j and class k, over encoder features z_j:
#     A_j       = tanh(V z_j) * sigmoid(U z_j)   # gated attention features (* = elementwise)
#     alpha_j^k = softmax_j( (W A_j)_k )         # attention distribution OVER TIME (sums to 1 per class)
#     p_j^k     = classifier(z_j)_k              # per-timestep class logit
#     Y^k       = sum_j alpha_j^k * p_j^k        # weighted average over time (length-invariant)
#   The extra sigmoid gate makes the attention richer than plain tanh attention. interpretation = alpha * p.
#
# Run it (WebTraffic by default):  python main.py run --model seanet/seanet_slim_gatedattn

name: seanet_slim_gatedattn
use_params: default

encoder:
  type: sea_mstcn_sep         # plain MSTCN-separable encoder (no gate) - clean baseline
  d: 64                   # channel width (the main size knob)
  n_blocks: 4             # how many multi-scale residual blocks are stacked
  dropout: 0.2
  max_dilation: 16        # dilation grows 1,2,4,8 (capped at 16) - keeps each view local
  kernels: [5, 11, 23]    # three depthwise kernel sizes, summed in every block

pooling:
  type: sea_gated_attention
  d_attn: 8               # width of the two attention branches (tanh V and sigmoid U)
  dropout: 0.2
  positional_encoding: true

training:
  n_epochs: 400
  patience: 60
  max_batch: 16
  learning_rate: 0.00125
  weight_decay: 0.00012
  label_smoothing: 0.13
  lambda_entropy: 0.01
  min_train_for_val: 100
  val_frac: 0.2
  optimizer: adam

# ===== AUTO-FILLED RESULTS (managed by main.py - do not edit below this line) =====
# These records let you compare the DEFAULT recipe against Optuna's BEST without opening MLflow,
# and (with `use_params: auto`) train with whichever scored higher. They are filled in for you:
#   records.default     : metrics from our last real `python main.py run` (default recipe).
#   records.optuna_best : the params Optuna found + their metrics (`python main.py optuna`).

records:
  default:
    metrics:
      test_acc: 0.894
      test_loss: 0.4027
      test_auroc: 0.9913
      dataset: WebTraffic
      recorded: '2026-08-03 17:14:19'
