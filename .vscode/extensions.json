{
  // VS Code shows these as "recommended" the first time the project is opened.
  // Nothing installs on its own - you still click Install.
  "recommendations": [
    "james-yu.latex-workshop",     // build the PDF + preview it + autocomplete
    "ltex-plus.vscode-ltex-plus",  // grammar and spell check inside .tex files
    "ms-python.python",
    "ms-toolsai.jupyter"
  ]
}
