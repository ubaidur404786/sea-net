# LaTeX build leftovers. They are rebuilt in seconds, so we do not track them.
# Do NOT delete them while working - LaTeX reuses them between passes.
*.aux
*.log
*.out
*.bbl
*.blg
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
main.pdf
