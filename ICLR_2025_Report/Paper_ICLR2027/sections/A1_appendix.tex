% !TEX root = ../main.tex
% =====================================================================
% APPENDICES -- after the references, unlimited length, but reviewers
% are not required to read them. So each one must be reachable from a
% specific sentence in the main text, and nothing that carries a claim
% may live here alone.
%
% Labels used by the main text:
%   app:encoders  app:hparams  app:cost  app:seeds
%   app:ablations app:published app:leaderboard
% =====================================================================

\section{The \seanet{} Encoder Variants}
\label{app:encoders}

All seven variants share the multi-scale separable block of \Cref{sec:design:enc} and change it at
one point. Each is stated as the problem it addresses and the modification.

\paragraph{Bottleneck.} The pointwise $1\times1$ step dominates the parameter count, costing
$\dmodel^2$ per unit. Factorise it through a narrow middle layer of width $r = \dmodel/4$:
$P = P_{\mathrm{expand}} P_{\mathrm{squeeze}}$, cost $\dmodel^2 \to 2\dmodel r = \dmodel^2/2$. At
$\dmodel = 64$ with 4 blocks this takes the whole model from $57{,}580$ to $41{,}324$ parameters, a
$28\%$ saving on the encoder. This is the smallest encoder here and the one in the headline row of
\Cref{tab:web}.

\paragraph{Gated.} Not every channel is informative for every series. Collapse the time axis to a
summary $\mathbf{s}$, form a gate $\mathbf{g} = \sig(W_g \mathbf{s}) \in [0,1]^{\dmodel}$, and
re-weight every step by $(1+\mathbf{g})$. The multiplier is $(1+\mathbf{g}) \in [1,2]$ rather than
$\mathbf{g}$ deliberately: a plain product could drive features to zero, whereas amplifying on top
of the original preserves the differences between time steps, which is what AOPCR and \ndcg{}
measure. Three summaries (max, mean, last) were trained.

\paragraph{Input-gated.} The gate above is one vector for the whole series. Compute it instead from
the raw input with a kernel-7 convolution, giving $\mathbf{g}_j$ per time step. The two variants
differ only in the subscript, so training both isolates the granularity of gating.

\paragraph{Spike/trend.} Convolution plus normalisation is biased towards smooth patterns. Run an
explicitly high-pass branch -- subtract a moving average of window $5$, convolve to $16$ channels --
and concatenate it with the trend features. The expectation was an advantage on impulsive evidence;
\Cref{tab:encoder_ablation} shows it was the weakest true encoder, so the expectation was not met.

\paragraph{Reconstruction-residual.} Rather than assuming what the encoder misses, measure it: a
$1\times1$ convolution reconstructs the raw input from the trend features, and the residual
$\bag - \widehat{\bag}$ becomes $16$ extra channels. There is no separate reconstruction loss, so
this is a learned residual feature and not an auto-encoder. It was the second strongest encoder.

\paragraph{Multi-scale channels (wrapper).} Append rolling maximum, minimum and standard deviation
over windows $\{3,7,15,31\}$ plus the sign of the first difference, widening the input from $1$ to
$14$ channels. Max, min and standard deviation are used rather than a rolling mean because a mean is
linear and the stem convolution could learn it unaided.

\paragraph{Multi-scale pyramid (wrapper).} Run one shared encoder on the series downsampled by
$\{1,2,4,8\}$, stretch the outputs back to length $\nsteps$, and fuse with attention over the
\emph{scale} axis, never over time -- the per-step signal the metrics depend on is preserved. This is
the one-dimensional analogue of multi-magnification whole-slide MIL \citep{hashimoto2020msdamil}. It
was the weakest variant built, and it imposes a constraint the pipeline checks automatically: the
wrapped encoder's receptive field must not exceed $\nsteps / 8$, or the coarse scales collapse.

\paragraph{Rejected alternatives.} A Transformer encoder was rejected on two grounds: full
self-attention costs $\mathcal{O}(\nsteps^2)$, prohibitive at $1{,}000$--$1{,}500$ steps on a
constrained device, and unrestricted attention lets any output position draw on any input position,
which diffuses the importance map. Pruning or quantising InceptionTime would reduce size but leaves
the global average pooling in place, so the model would still need a post-hoc explainer.

\section{Full Hyperparameters}
\label{app:hparams}

\begin{table}[htbp]
\caption{Every setting shared across the study, plus the two encoder capacities. The head-specific
initial values are the constructor defaults and are not overridden by any configuration file.}
\label{tab:hparams}
\begin{center}
\footnotesize
\begin{tabular}{lll}
\toprule
\textbf{Group} & \textbf{Parameter} & \textbf{Value} \\
\midrule
Optimisation & optimiser & Adam \citep{kingma2015adam} \\
             & learning rate / weight decay & $1.25\times10^{-3}$ / $1.2\times10^{-4}$ \\
             & epochs / patience & max $400$ / $60$ \\
             & batch size & $\min(\lfloor n_{\mathrm{train}}/10\rfloor, 16)$, floor $2$ \\
             & validation split & stratified $80/20$ if $n_{\mathrm{train}} \geq 100$ \\
             & label smoothing & $0.13$ \\
             & $\lambda_{\mathrm{focus}}$ (\Cref{eq:loss}) & $0.01$; $0$ for the \millet{} baseline \\
             & seeds & $3$ for four models, $1$ for the other $68$ \\
\midrule
Encoder, narrow & width $\dmodel$ / blocks & $64$ / $4$ \\
Encoder, wide   & width $\dmodel$ / blocks & $128$ / $6$ \\
\emph{both}     & kernels $\mathcal{K}$ / dilation cap / dropout & $\{5,11,23\}$ / $16$ / $0.2$ \\
\midrule
Pooling & attention width $\dattn$ / dropout & $8$ / $0.2$ \\
        & Top-$k$ fraction $\kappa$ & $0.1$ \\
        & initial $\tau_0$ (softmax conjunctive) & $1.0$ \\
        & initial $\beta_0$ (adaptive class-wise) & $0.3$ \\
        & initial $\lambda_0$ (attention-max / dual-stream) & $0.5$ / $0.05$ \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\section{Cost Measurements}
\label{app:cost}

\begin{table}[htbp]
\caption{Measured on the real \webtraffic{} input (length $1{,}008$, $10$ classes, batch $32$) on one
GPU. ``Train'' is wall-clock time to convergence including early stopping, mean $\pm$ sd over the
three \webtraffic{} seeds where available. The last two columns are the ones that do not favour the
small models and are discussed in \Cref{sec:cheap:cost}.}
\label{tab:cost}
\begin{center}
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrrrrr}
\toprule
\textbf{Model} & \textbf{Params} & \textbf{Size} & \textbf{FLOPs} &
\textbf{1 pred.} & \textbf{Peak mem} & \textbf{Train} \\
 & & (MB) & (M) & (ms) & (MB) & (s) \\
\midrule
\seanet{} bottleneck $+$ Top-$k$ & \textbf{41{,}324} & \textbf{1.43} & \textbf{76.7} & 0.131 & 179.0 & $117.3\pm14.0$ \\
\seanet{} gated $+$ Top-$k$      & 61{,}740  & 1.50 & 109.7 & 0.128 & \textbf{64.9} & $91.3\pm25.9$ \\
\seanet{} wide $+$ class-wise    & 269{,}164 & 3.54 & 523.7 & 0.359 & 123.7 & --- \\
\millet{}                        & 423{,}707 & 4.11 & 847.6 & 0.203 & 112.3 & $50.4\pm9.5$ \\
ResNet $+$ conjunctive           & 506{,}331 & 4.42 & 1013.2 & 0.139 & 76.5 & 38.1 \\
FCN $+$ conjunctive              & 267{,}035 & 3.47 & 535.2 & \textbf{0.068} & 132.2 & 20.8 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\section{Per-Seed Results}
\label{app:seeds}

\begin{table}[htbp]
\caption{\webtraffic{} accuracy for the four models trained with three seeds. These four spreads are
what the rule of \Cref{sec:protocol} is calibrated against.}
\label{tab:seeds}
\begin{center}
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{Seed 0} & \textbf{Seed 1} & \textbf{Seed 2} & \textbf{Mean $\pm$ sd} \\
\midrule
\seanet{} gated $+$ Top-$k$        & 0.954 & 0.958 & 0.952 & $0.955\pm0.003$ \\
\seanet{} bottleneck $+$ Top-$k$   & 0.938 & 0.938 & 0.966 & $0.947\pm0.016$ \\
\seanet{} input-gated $+$ adaptive & 0.938 & 0.880 & 0.898 & $0.905\pm0.030$ \\
\millet{}, re-trained here         & 0.894 & 0.876 & 0.892 & $0.887\pm0.010$ \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

The three behave differently, and this matters for how far each result travels. The gated model is
stable, six thousandths between best and worst seed. The bottleneck model is tight on two seeds and
jumps on the third, so its mean is raised by one good run. The input-gated model is genuinely
dispersed, which is why its AOPCR advantage in \Cref{tab:web} is read as noise.

\section{Encoder Ablation}
\label{app:ablations}

\begin{table}[htbp]
\caption{\webtraffic{} accuracy, AOPCR and \ndcg{} for each encoder, averaged over the $n$ pooling
heads it was paired with; $^{\dagger}$ marks a wrapper. The companion table on the head axis is
\Cref{tab:pooling}. Two cautions: several groups are small, and the bottleneck encoder's mean is
dragged down because it was also paired with the weak heads -- its best pairing is the headline row
of \Cref{tab:web}.}
\label{tab:encoder_ablation}
\begin{center}
\footnotesize
\begin{tabular}{lcccr}
\toprule
\textbf{Encoder} & \textbf{Mean acc.} & \textbf{Mean AOPCR} & \textbf{Mean \ndcg{}} & \textbf{$n$} \\
\midrule
\seanet{} multi-scale channels$^{\dagger}$ & \textbf{0.937} & 2.385 & \textbf{0.747} & \phantom{0}4 \\
\seanet{} reconstruction-residual          & 0.924 & 2.315 & 0.719 & \phantom{0}5 \\
\seanet{} input-gated                      & 0.904 & 2.345 & 0.718 & \phantom{0}5 \\
\seanet{} gated                            & 0.902 & 2.056 & 0.710 & 19 \\
\seanet{} base                             & 0.898 & 1.933 & 0.694 & 12 \\
\seanet{} bottleneck                       & 0.884 & 2.209 & 0.694 & \phantom{0}8 \\
\seanet{} spike/trend                      & 0.858 & 1.814 & 0.677 & \phantom{0}7 \\
\seanet{} multi-scale pyramid$^{\dagger}$  & 0.771 & 1.394 & 0.604 & \phantom{0}3 \\
\midrule
InceptionTime (\millet{})                  & 0.887 & 2.569 & 0.677 & \phantom{0}1 \\
ResNet                                     & 0.772 & 2.952 & 0.554 & \phantom{0}1 \\
FCN                                        & 0.742 & 3.826 & 0.533 & \phantom{0}1 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

The encoder spread is $0.771$--$0.937$ ($0.166$) against the head spread of $0.744$--$0.921$
($0.177$) in \Cref{tab:pooling}. Those two numbers are the evidence for \Cref{sec:cheap:axes}.

\section{Relation to the Published \millet{} Results}
\label{app:published}

The main text compares only models re-trained inside this harness. This appendix answers the
separate question that choice raises: how do those numbers relate to the ones \millet{} published,
and is the difference caused by our re-implementation or by the training configuration?

\begin{table}[htbp]
\caption{The \millet{} baseline (InceptionTime $+$ conjunctive pooling) measured five ways. Only the
last row is used anywhere in the main text, except in \Cref{tab:aopcr} and \Cref{tab:ucr} where the
fourth row appears explicitly as the alternative recipe.}
\label{tab:published}
\begin{center}
\footnotesize
\begin{tabular}{lcc}
\toprule
\textbf{How the number was obtained} & \textbf{\webtraffic{} acc.} & \textbf{\ucr{}-85 mean acc.} \\
\midrule
Published \citep{early2024millet}, mean of $5$ seeds  & 0.924 & 0.8445 \\
Published \citep{early2024millet}, $5$-model ensemble & 0.940 & --- \\
Their released weights, evaluated in this harness     & 0.922 & --- \\
Re-trained here under \emph{their} hyperparameters    & 0.920 & 0.8434 \\
\midrule
\textbf{Re-trained here under \Cref{sec:protocol}}    & \textbf{0.887} & \textbf{0.8274} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Two conclusions follow. First, the re-implementation is correct: under \millet{}'s own
hyperparameters it reproduces their \ucr{} mean to within about one thousandth ($0.8434$ against
$0.8445$), with a balanced per-dataset record of $26$ wins, $32$ ties and $26$ losses against the
published numbers, and their released weights evaluated in our harness land inside the spread of
their own five seeds. Second, the remaining difference is therefore the training budget, and not the
architecture or the code -- about $0.016$ on the \ucr{} mean and $0.033$ on \webtraffic{}. That
figure cannot simply be added to our models, since a longer schedule need not help every
architecture equally, but it is the right order of magnitude to hold in mind while reading
\Cref{tab:ucr}. The AOPCR values of these same two runs are what \Cref{sec:aopcr} is built on.

\section{Full Leaderboard and Per-Dataset Results}
\label{app:leaderboard}

The complete table of all $72$ encoder $\times$ head combinations -- with the configuration
identifier for every row, so that any row can be reproduced by name -- together with the full
encoder $\times$ head grid, the per-dataset \ucr{} results for every model, the win/tie/loss
analysis and the critical-difference diagram, is published with the code at \anonrepo{}. It is
omitted here only for length; nothing in the main text depends on a number that appears solely in
that material.
